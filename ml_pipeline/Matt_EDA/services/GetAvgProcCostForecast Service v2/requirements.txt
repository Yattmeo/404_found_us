# GetM9MonthlyCostForecast Service — Python dependencies
# All versions pinned to ranges that are stable as of 2025.

fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
pydantic>=2.9.0,<3.0.0
numpy>=1.26.0,<3.0.0
scikit-learn>=1.5.0,<2.0.0
joblib>=1.4.0,<2.0.0
pandas>=2.2.0,<3.0.0        # required by train.py

# Dev / test extras (not needed for the service at runtime; install via
#   pip install -r requirements.txt -r requirements-dev.txt)
# pytest>=8.2.0
# httpx>=0.27.0             # for TestClient
