services:
  postgres:
    image: postgres:16
    container_name: ml-postgres
    environment:
      - POSTGRES_USER=pguser
      - POSTGRES_PASSWORD=pgpassword
      - POSTGRES_DB=mldb
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped
    networks:
      - ml-network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: ml-backend
    environment:
      - DATABASE_URL=postgresql://pguser:pgpassword@postgres:5432/mldb
      - COST_STRUCTURE_DIR=/app/cost_structure
      - ML_SERVICE_URL=http://ml-service:8001
    volumes:
      - ./backend:/app
      - ./cost_structure:/app/cost_structure:ro
    depends_on:
      - postgres
    restart: unless-stopped
    networks:
      - ml-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: ml-frontend
    environment:
      - REACT_APP_BACKEND_URL=http://localhost/api/v1
      - PUBLIC_URL=/sales
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - backend
    restart: unless-stopped
    networks:
      - ml-network

  merchant-frontend:
    build:
      context: ./merchant-frontend
      dockerfile: Dockerfile
    container_name: ml-merchant-frontend
    depends_on:
      - backend
    restart: unless-stopped
    networks:
      - ml-network

  nginx:
    image: nginx:alpine
    container_name: ml-nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - backend
      - frontend
      - merchant-frontend
      - ml-service
    restart: unless-stopped
    networks:
      - ml-network

  ml-service:
    build:
      context: ./ml_service
      dockerfile: Dockerfile
    container_name: ml-service
    environment:
      - DATABASE_URL=postgresql://pguser:pgpassword@postgres:5432/mldb
      - M9_ARTIFACTS_BASE_PATH=/app/artifacts/m9
      - TPV_ARTIFACTS_BASE_PATH=/app/artifacts/tpv
    volumes:
      - ./ml_service:/app
      - ./ml_pipeline/Matt_EDA/services/KNN Quote Service Production/processed_transactions_4mcc.csv:/data/processed_transactions_4mcc.csv:ro
    depends_on:
      - postgres
    restart: unless-stopped
    networks:
      - ml-network

networks:
  ml-network:
    driver: bridge

volumes:
  postgres_data: